apply plugin: 'java'

dependencies {
    compile files("${System.properties['java.home']}/lib/jfxrt.jar")
}

task demoOne (type: JavaExec) {
    main = 'com.github.shemnon.deckcontrol.DemoOne'
    classpath = sourceSets.test.runtimeClasspath
}